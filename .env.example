# Terminal Claude Code Web - Docker Compose Environment Variables

# Session Secret (generate a new one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
SESSION_SECRET=your_very_long_and_random_session_secret_here

# Node Environment (development or production)
NODE_ENV=development
